import { updateValue } from "./functions";

export const DATA = {
    inputType: 'text'
}

export const PROPS = ['type', 'value', 'placeholder', 'disabled', 'label', 'min'];
export const METHODS = {
    updateValue
}