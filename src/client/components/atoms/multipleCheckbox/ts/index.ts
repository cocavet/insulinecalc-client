import { updateValue } from "./functions";

export const DATA = {
    type: 'checkbox',
    value: ''
}
export const PROPS = ['options', 'label', 'checkedValue'];
export const METHODS = {
    updateValue
}